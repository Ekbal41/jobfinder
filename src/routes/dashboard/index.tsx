import { component$ } from "@builder.io/qwik";
export default component$(() => {
  return (
    <div class="flex  items-center  justify-center gap-3 py-20">
      <p class="w-64">
        You have no applications yet.
        <br /> Apply to a job to get started.🚀
      </p>
    </div>
  );
});
